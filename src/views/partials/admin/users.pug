h2.h2.mt-4 Users
ul 
  each user in users 
    li(class="p-2 bg-blue-100 rounded-xl my-4") 
      p(class="font-bold") #{user.name}
      p 
        strong Email: 
        =user.email
      if user.level
        p 
          strong Level id:  
          =user.level
